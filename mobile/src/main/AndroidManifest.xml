<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chaos.reactive">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/application"
        android:name="com.chaos.reactive.ReactiveApplication"
        android:theme="@style/Theme.Reactive">

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="0ffc21fb83ec380fad92dda78c9be0ffd107e875" />

        <activity
            android:name=".ui.MainActivity"
            android:label="@string/application"
            android:theme="@style/Theme.Reactive.Dark">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

    </application>

</manifest>
